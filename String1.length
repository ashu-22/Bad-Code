#include<stdio.h>
#include<conio.h>
void main()
{
String str1;
String str2;
String str1 = new String(str1);
String str2 = new String(str2);
for(int i = 0; i < str1.length; i++)
{
for(int j = 0; j < str2.length; j++)
{
System.out.println("str1.length is length: " + length);
System.out.println("str2.length is length: " + length);
}
}
}
